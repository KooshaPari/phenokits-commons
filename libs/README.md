# Libraries

Runtime libraries were removed from  on 2026-06-20 to keep this repository scoped to governance, templates, policies, docs, and shared artifact patterns.

Absorbed homes:

- Go libs:  PR #21
- Python :  PR #28
- Python libs already equivalent in 
- TypeScript libs: preserved in  under 

Do not add runtime package implementations here. Use a tight SDK/package repo instead.
